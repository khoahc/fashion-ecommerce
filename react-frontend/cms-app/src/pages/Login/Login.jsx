import { useEffect } from "react";

const Login = ({ setToken }) => {

    useEffect(() => {
        document.title = 'Đăng nhập';
      });

  return (
    <div>Login</div>
  )
}

export default Login